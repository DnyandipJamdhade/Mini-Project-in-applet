# Mini-Project-in-applet
A simple Java AWT-based login screen. Users enter a username and password, which must match. If not, a custom exception is raised. Users get 3 login attempts. A "Clear" button resets the fields. Demonstrates GUI design, event handling, and basic validation using AWT.
